// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// export default nextConfig;
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "picsum.photos",
      "fastly.picsum.photos",
      "simg.ssgcdn.com",
      "sitem.ssgcdn.com",
    ],
  },
};

export default nextConfig;
